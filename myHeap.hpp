// myHeap.hpp
#ifndef MYHEAP_HPP
#define MYHEAP_HPP

#include <vector>
#include <iostream>
#include <queue>
#include <vector>
#include <functional> // For std::greater

void heapMedian(const std::vector<int>* instructions);

#endif // MYHEAP_HPP
